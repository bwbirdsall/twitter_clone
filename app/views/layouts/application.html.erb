<!DOCTYPE html>
<html>
<head>
  <title>Pseudotwitter</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>

<div class="container">

  <div class="navbar navbar-default navbar-fixed-top">
    <%= link_to "PseudoTwitter", root_url, class: "navbar-text pull-left" %>
    <% if user_signed_in? %>
      <%= link_to "Logout", destroy_user_session_path, method: :delete, class: "navbar-text pull-right" %>
      <%= link_to 'Edit profile', edit_user_registration_path, class: "navbar-text pull-right" %>
      <span class="navbar-text pull-right">Logged in as <strong><%= current_user.handle %></strong>.</span>
    <% else %>
      <%= link_to "Login", new_user_session_path, class: "navbar-text pull-right" %>
      <%= link_to "Sign up", new_user_registration_path, class: "navbar-text pull-right" %>
    <% end %>
  </div>

  <div class="content">
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>

    <%= yield %>
  </div>
</div>

</body>
</html>
